# B1 – Beginne bei `(20, 20)` und zeichne mit einer `for`-Schleife eine Treppe mit fünf Stufen aus `right(5)` und `down(5)`.

@difficulty:einfach

**⭐ B1** Beginne bei `(20, 20)` und zeichne mit einer `for`-Schleife eine Treppe mit fünf Stufen aus `right(5)` und `down(5)`.
