# 10.7 – Definiere `bewegung_erlaubt(richtung)`. Eine festgelegte Farbe gilt als Wand; prüfe die Farbe des Nachbarfelds mit `get_color()`.

@difficulty:fortgeschritten

**⭐⭐⭐ 10.7** Definiere `bewegung_erlaubt(richtung)`. Eine festgelegte Farbe gilt als Wand; prüfe die Farbe des Nachbarfelds mit `get_color()`.
